<!DOCTYPE html>
<html>
<head>
  <title><%= post.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="max-w-4xl mx-auto p-6">
    <h1 class="text-3xl font-bold mb-4"><%= post.title %></h1>
    <p class="text-sm text-gray-500 mb-4">By <%= post.user.username %></p>

    <p class="mb-4"><%= post.content1 %></p>
    <% if (post.content2) { %>
      <p class="mb-4"><%= post.content2 %></p>
    <% } %>

    <div class="flex gap-4">
      <% if (post.pic1) { %>
        <img src="/uploads/<%= post.pic1 %>" class="w-60 h-40 object-cover rounded-lg">
      <% } %>
      <% if (post.pic2) { %>
        <img src="/uploads/<%= post.pic2 %>" class="w-60 h-40 object-cover rounded-lg">
      <% } %>
    </div>
  </div>
</body>
</html>
